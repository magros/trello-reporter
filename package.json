{
  "name": "trello-reporter",
  "version": "1.0.0",
  "description": "A scrum trello reporter",
  "main": "reporter.js",
  "dependencies": {
    "nodemailer": "^5.1.1",
    "request": "^2.88.0",
    "request-promise": "^4.2.2"
  },
  "devDependencies": {
    "eslint": "^5.13.0",
    "eslint-config-strongloop": "^2.1.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pretest": "eslint --ignore-path .gitignore ."
  },
  "author": "Marco Antonio Godinez Rosas",
  "license": "ISC"
}
